import 'package:flutter/material.dart';

class ReceptionistBookScreen extends StatelessWidget {
  const ReceptionistBookScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}